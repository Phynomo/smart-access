import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phynomo.smartacess',
  appName: 'SmartAccess',
  webDir: 'dist/smart-access-app/browser'
};

export default config;
